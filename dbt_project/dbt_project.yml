name: 'saas_feature_adoption'
version: '1.0.0'

profile: 'saas_feature_adoption'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  saas_feature_adoption:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
